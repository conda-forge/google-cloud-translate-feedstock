schema_version: 1

context:
  name: google-cloud-translate
  version: "3.25.0"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/google_cloud_translate-${{ version }}.tar.gz
  sha256: a3401e20e3d18eeab68e15b80faf4f985c6ca21934affea4038b961cd19310de

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools
  run:
    - python >=${{ python_min }}
    - google-api-core >=2.11.0,<3.0.0
    - google-api-core-grpc
    - google-auth >=2.14.1,<3.0.0,!=2.24.0,!=2.25.0
    - google-cloud-core >=2.0.0,<3.0.0
    # more restrictive constraint for python >=3.14
    - grpcio >=1.75.1,<2.0.0
    # more restrictive constraint for python >=3.13
    - proto-plus >=1.25.0,<2.0.0
    - protobuf >=4.25.8,<8.0.0
    - grpc-google-iam-v1 >=0.14.0,<1.0.0

tests:
  - python:
      imports:
        - google.cloud.translate
        - google.cloud.translate_v2
        - google.cloud.translate_v3beta1
        - google.cloud.translate_v3
        - grpc
      python_version: ${{ python_min }}.*
      pip_check: true

about:
  summary: Google Cloud Translate API client library
  license: Apache-2.0
  license_file: LICENSE
  homepage: https://github.com/googleapis/google-cloud-python/tree/main/packages/google-cloud-translate
  repository: https://github.com/googleapis/google-cloud-python/tree/main/packages/google-cloud-translate
  documentation: https://googleapis.dev/python/translation/latest/index.html

extra:
  recipe-maintainers:
    - xylar
    - BrentDorsey
    - parthea
    - xylar
